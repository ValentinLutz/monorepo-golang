# Global config
region: NONE
environment: LOCAL

# Server config
server:
  port: 10443
  certificate_path: config/app.crt
  key_path: config/app.key

# Logger config
logger:
  level: DEBUG

# Database config
database:
  host: localhost
  port: 5432
  user: test
  password: test
  database: test
  max_idle_connections: 4
  max_open_connections: 8
  max_idle_time: 5m
  max_lifetime: 1h
