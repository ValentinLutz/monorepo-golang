# Global config
region: NONE
environment: CONTAINER

# Server config
server:
  port: 8433
  timeout:
    read: 10
    write: 10
    idle: 10

# Logger config
logger:
  level: INFO
  json: false

# Database config
database:
  host: postgresql
  port: 5432
  user: "test"
  password: "test"
  database: "dev_db"
  max_idle_connections: 10
  max_open_connections: 90
